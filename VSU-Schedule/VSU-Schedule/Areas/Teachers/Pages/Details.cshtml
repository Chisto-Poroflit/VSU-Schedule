@page
@model VSU_Schedule.Areas.Teachers.Pages.DetailsModel

@{
    ViewData["Title"] = Html.DisplayFor(m=>m.Teacher.FullName);
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h4>@Model.Teacher.FullName</h4>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Teacher.FullName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Teacher.FullName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Teacher.Position)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Teacher.Position)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Teacher.Id">Редактировать</a> |
    <a asp-page="./Index">Назад к списку</a>
</div>
